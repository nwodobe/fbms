/* ANAGROCI Operations Suite - Sacherie SOP-006 integree a la coquille Pilotage */
(function(){
'use strict';
if(window.__ANAGROCI_SACHERIE_V2)return;window.__ANAGROCI_SACHERIE_V2=true;
var SB=null,AUTH=null,HOST=null,MODE='requests';
var STATE={rt:[],avances:[],requests:[],calc:null,context:{},prefill:{}};
var DRAFT_KEY='anagroci_sacherie_v2_drafts';
var MODULE=(window.ANAGROCI_MODULE||(window.ANAGROCI_AUTH&&window.ANAGROCI_AUTH.module)||'');
function $(id){return document.getElementById(id);}
function esc(s){return String(s==null?'':s).replace(/[&<>"']/g,function(c){return {'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c];});}
function fmt(v){return Number(v||0).toLocaleString('fr-FR');}
function num(v){var n=Number(String(v==null?'':v).replace(',','.'));return Number.isFinite(n)?n:null;}
function op(){return String(STATE.context&&STATE.context.fonction_operationnelle||'');}
function userCluster(){return String(STATE.context&&STATE.context.cluster||'');}
function isBM(){return !!(AUTH&&AUTH.profile&&AUTH.profile.role==='Branch Manager');}
function canRequest(){return isBM()||op()==='Unit Head'||op()==='Assistant Unit Head';}
function canExecuteRequest(r){return isBM()||((op()==='Warehouse Keeper'||op()==='Assistant Unit Head')&&sameCluster(userCluster(),r&&r.cluster));}
function audit(action,details){try{window.ANAGROCI_AUDIT&&window.ANAGROCI_AUDIT.log(action,details||{});}catch(e){}}
function client(){if(SB)return SB;if(window.supabase&&window.ANAGROCI_SUPABASE_URL&&window.ANAGROCI_SUPABASE_ANON)SB=window.supabase.createClient(window.ANAGROCI_SUPABASE_URL,window.ANAGROCI_SUPABASE_ANON);return SB;}
function readDrafts(){try{return JSON.parse(localStorage.getItem(DRAFT_KEY)||'[]');}catch(e){return [];}}
function writeDrafts(v){try{localStorage.setItem(DRAFT_KEY,JSON.stringify(v));return true;}catch(e){return false;}}
function uid(prefix){try{return (prefix||'req')+'-'+crypto.randomUUID();}catch(e){return (prefix||'req')+'-'+Date.now()+'-'+Math.round(Math.random()*1e9);}}
function friendly(err){var s=String(err&&err.message||err&&err.details||err||'');if(/approval|approb/i.test(s))return 'Mouvement bloqué : approbation Branch Manager requise.';if(/plafond|10%|autorise|autoris|disponible/i.test(s))return 'Demande bloquée : quantité supérieure au plafond disponible.';if(/stock sacs|insuffisant/i.test(s))return 'Mouvement bloqué : stock sacs cluster insuffisant.';if(/expire/i.test(s))return 'Approbation expirée : créez une nouvelle demande.';if(/cluster/i.test(s)&&/hors|attribu|diff/i.test(s))return 'Opération bloquée : RT ou mouvement hors du cluster attribué.';if(/network|fetch/i.test(s))return 'Réseau indisponible. La demande peut être gardée en brouillon local.';return s||'Opération impossible.';}
function labelRT(r){return r.nom||(r.data&&(r.data.nom||r.data.rt||r.data.nomComplet))||(r.village_nom?('RT '+r.village_nom):'RT');}
function clusterRT(r){return r.cluster||(r.data&&r.data.cluster)||'';}
function sameCluster(a,b){return String(a||'').toUpperCase()===String(b||'').toUpperCase();}
function age(v){if(!v)return 'date inconnue';var ms=Date.now()-new Date(v).getTime();if(!Number.isFinite(ms))return 'date inconnue';var h=Math.max(0,Math.floor(ms/3600000));return h<48?'il y a '+h+' h':'il y a '+Math.floor(h/24)+' j';}
function remaining(v){if(!v)return 'échéance inconnue';var ms=new Date(v).getTime()-Date.now();if(!Number.isFinite(ms))return 'échéance inconnue';if(ms<=0)return 'expirée';var h=Math.ceil(ms/3600000);return h<48?'expire dans '+h+' h':'expire dans '+Math.ceil(h/24)+' j';}
function css(){if($('sv2_style'))return;var s=document.createElement('style');s.id='sv2_style';s.textContent='\
.sv2-shell{background:#fff;border:1px solid #E8E7E7;border-radius:12px;padding:15px;margin-bottom:12px}.sv2-headline{display:flex;gap:10px;align-items:center;flex-wrap:wrap;margin-bottom:12px}.sv2-headline h2{font:750 16px Archivo,Arial;color:#053B23;margin:0}.sv2-headline small{color:#6F746F}.sv2-mode{margin-left:auto;min-height:40px;border:1px solid #D2D0D0;border-radius:8px;padding:7px 9px;background:#fff}.sv2-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(190px,1fr));gap:10px}.sv2-field{display:flex;flex-direction:column;gap:5px}.sv2-field label{font-size:12px;font-weight:800}.sv2-field input,.sv2-field select,.sv2-field textarea{width:100%;border:1px solid #D2D0D0;border-radius:8px;padding:10px 11px;font:14px "IBM Plex Sans",Arial;background:#fff}.sv2-field input:focus,.sv2-field select:focus,.sv2-field textarea:focus,.sv2-mode:focus{outline:3px solid rgba(141,197,86,.45);outline-offset:1px}.sv2-preview{margin-top:12px;border:1px solid #BFE3B6;background:#F1F9EF;border-radius:10px;padding:12px;line-height:1.5;font-size:13px}.sv2-preview.bad{border-color:#E7B7B0;background:#FFF1F1}.sv2-preview.warn{border-color:#EE9E00;background:#FDEFCE}.sv2-actions{display:flex;gap:8px;flex-wrap:wrap;margin-top:12px}.sv2-btn{min-height:40px;border:0;border-radius:8px;padding:9px 12px;font-weight:800;cursor:pointer;background:#008F37;color:#fff}.sv2-btn:focus-visible{outline:3px solid #8DC556;outline-offset:2px}.sv2-btn:disabled{opacity:.45}.sv2-btn.ghost{background:#fff;color:#053B23;border:1px solid #D2D0D0}.sv2-btn.warn{background:#EE9E00;color:#2D2400}.sv2-btn.danger{background:#C0392B}.sv2-note{font-size:12px;color:#6B756E;line-height:1.5;margin-top:8px}.sv2-msg{margin-top:10px;padding:10px;border-radius:9px;font-size:13px;display:none}.sv2-msg.ok{display:block;background:#F1F9EF;color:#1C4B30;border:1px solid #BFE3B6}.sv2-msg.err{display:block;background:#FFF1F1;color:#8F2D22;border:1px solid #E7B7B0}.sv2-table{overflow:auto}.sv2-table table{width:100%;border-collapse:collapse;font-size:12px;min-width:650px}.sv2-table th{font-size:11px;text-transform:uppercase;letter-spacing:.04em;color:#5F665F;text-align:left;padding:8px;border-bottom:2px solid #008F37}.sv2-table td{padding:9px 8px;border-bottom:1px solid #EEE;vertical-align:top}.sv2-pill{display:inline-block;border-radius:999px;padding:4px 8px;font-size:11px;font-weight:800}.sv2-pending{background:#FDEFCE;color:#80600F}.sv2-approved{background:#E3F5E8;color:#1C7A3F}.sv2-hold{background:#FDECE9;color:#A83225}.sv2-closed{background:#F0EFED;color:#4F4E4E}.sv2-empty{padding:20px;text-align:center;color:#7A7878}.sv2-kpi{display:grid;grid-template-columns:repeat(auto-fit,minmax(135px,1fr));gap:8px;margin:10px 0}.sv2-kpi div{background:#F7FAF6;border:1px solid #E8E7E7;border-radius:9px;padding:9px}.sv2-kpi small{display:block;color:#7A7878}.sv2-kpi b{color:#053B23;font-size:17px;font-variant-numeric:tabular-nums}@media(max-width:520px){.sv2-shell{padding:12px}.sv2-headline .sv2-mode{width:100%;margin-left:0}}';document.head.appendChild(s);}
function toast(text,kind){var el=$('sv2_msg');if(!el)return;el.className='sv2-msg '+(kind||'ok');el.textContent=text;}
function visibleRT(){if(isBM()||!userCluster())return STATE.rt;if(op()==='Unit Head'||op()==='Assistant Unit Head'||op()==='Warehouse Keeper')return STATE.rt.filter(function(r){return sameCluster(clusterRT(r),userCluster());});return STATE.rt;}
function cycleOptions(rtId){return STATE.avances.filter(function(a){return String(a.rt_id)===String(rtId)&&a.cycle_id&&Number(a.volume_finance_kg)>0&&a.cycle_statut==='OPEN';});}
function rtOptions(){return '<option value="">Choisir un RT</option>'+visibleRT().map(function(r){return '<option value="'+esc(String(r.id))+'" '+(String(STATE.prefill.rt_id||'')===String(r.id)?'selected':'')+'>'+esc(labelRT(r))+(clusterRT(r)?' - '+esc(clusterRT(r)):'')+'</option>';}).join('');}
function statusPill(s){var c=/APPROVED|EXECUTED/.test(s)?'sv2-approved':/HOLD|REJECTED|FAIL/.test(s)?'sv2-hold':/CLOSED/.test(s)?'sv2-closed':'sv2-pending';return '<span class="sv2-pill '+c+'">'+esc(s||'-')+'</span>';}
function dispatchCounts(){var pending=STATE.requests.filter(function(r){return r.status==='PENDING_BM'||r.status==='HOLD';}).length,expiring=STATE.requests.filter(function(r){return r.status==='APPROVED'&&r.expires_at&&new Date(r.expires_at).getTime()>Date.now()&&new Date(r.expires_at).getTime()-Date.now()<48*3600000;}).length;window.dispatchEvent(new CustomEvent('sacherie:v2counts',{detail:{requests:pending,expiring:expiring}}));window.dispatchEvent(new CustomEvent('sacherie:counts',{detail:{requests:pending,losses:(window.ANAGROCI_SACHERIE_CT_ACTIONS&&window.ANAGROCI_SACHERIE_CT_ACTIONS.pendingCount?window.ANAGROCI_SACHERIE_CT_ACTIONS.pendingCount():0),expiring:expiring}}));}
async function loadAll(){var c=client();if(!c)return;try{var cx=await c.rpc('sacherie_mon_contexte');if(!cx.error)STATE.context=cx.data||{};var rr=await c.from('rt').select('id,nom,cluster,data,village_nom,statut').eq('deleted',false);if(!rr.error)STATE.rt=rr.data||[];var av=await c.from('avances').select('id,date,cluster,rt_id,rt_nom,montant,statut,cycle_id,volume_finance_kg,prix_reference_kg,cycle_statut,created_at').order('created_at',{ascending:false}).limit(300);if(!av.error)STATE.avances=av.data||[];var rq=await c.from('bag_movement_requests').select('*').order('requested_at',{ascending:false}).limit(200);if(!rq.error)STATE.requests=rq.data||[];}catch(e){}dispatchCounts();if(HOST&&document.body.contains(HOST))renderHost();window.dispatchEvent(new CustomEvent('sacherie:ready'));}
function modeOptions(){var opts=[['request','Nouvelle demande'],['requests','Demandes'],['approved','À remettre']];if(isBM())opts.push(['decisions','Décisions BM']);return opts.map(function(x){return '<option value="'+x[0]+'" '+(MODE===x[0]?'selected':'')+'>'+x[1]+'</option>';}).join('');}
function renderInto(host){if(!host)return;HOST=host;css();if(!MODE)MODE='requests';renderHost();}
function renderHost(){if(!HOST)return;HOST.innerHTML='<section class="sv2-shell"><div class="sv2-headline"><div><h2>Flux de dotation SOP-006</h2><small>Demande → décision → remise → traçabilité</small></div><select id="sv2_mode" class="sv2-mode" aria-label="Vue du workflow">'+modeOptions()+'</select></div><div id="sv2_panel"></div><div id="sv2_msg" class="sv2-msg"></div></section><div id="sv2_loss_decisions"></div>';var mode=$('sv2_mode');if(mode)mode.onchange=function(){MODE=mode.value;renderHost();};renderPanel();if(window.ANAGROCI_SACHERIE_CT_ACTIONS&&window.ANAGROCI_SACHERIE_CT_ACTIONS.renderLossDecisionsInto)window.ANAGROCI_SACHERIE_CT_ACTIONS.renderLossDecisionsInto($('sv2_loss_decisions'));}
function renderPanel(){var p=$('sv2_panel');if(!p)return;if(MODE==='request')renderRequestForm(p);else if(MODE==='approved')renderApproved(p);else if(MODE==='decisions')renderApprovals(p);else renderRequests(p);}
function renderRequestForm(p){if(!canRequest()){p.innerHTML='<div class="sv2-empty">Votre fonction opérationnelle ne permet pas de créer une demande. Fonction attendue : Unit Head / Assistant Unit Head, ou Branch Manager.</div>';return;}var drafts=readDrafts();p.innerHTML='<div class="sv2-grid"><div class="sv2-field"><label>RT</label><select id="sv2_rt">'+rtOptions()+'</select></div><div class="sv2-field"><label>Cycle financé OPEN</label><select id="sv2_cycle"><option value="">Choisir d’abord le RT</option></select></div><div class="sv2-field"><label>Stock RCN physique vérifié (kg)</label><input id="sv2_stock" type="number" min="0" step="1" placeholder="Saisir le comptage physique"></div><div class="sv2-field"><label>Quantité demandée (sacs)</label><input id="sv2_qty" type="number" min="1" step="1"></div></div><div id="sv2_preview" class="sv2-preview warn">Sélectionnez un RT et un cycle. Le plafond sera calculé par le serveur.</div><div class="sv2-actions"><button class="sv2-btn ghost" id="sv2_calc">Calculer le plafond</button><button class="sv2-btn" id="sv2_submit" disabled>Soumettre au BM</button>'+(drafts.length?'<button class="sv2-btn warn" id="sv2_syncdraft">Envoyer '+drafts.length+' brouillon(s)</button>':'')+'</div><div class="sv2-note"><b>Règle :</b> 80 kg / sac · marge max 10 %. Le stock RCN provient d’un comptage physique. Une demande locale n’est jamais une approbation.</div>';var rt=$('sv2_rt');rt.onchange=onRequestRt;$('sv2_calc').onclick=calculate;$('sv2_qty').oninput=renderCalc;$('sv2_submit').onclick=submitRequest;if($('sv2_syncdraft'))$('sv2_syncdraft').onclick=syncDrafts;if(rt.value)onRequestRt();}
function onRequestRt(){var rt=$('sv2_rt').value,list=cycleOptions(rt),sel=$('sv2_cycle');sel.innerHTML='<option value="">Choisir un cycle OPEN</option>'+list.map(function(a){return '<option value="'+esc(a.cycle_id)+'">'+esc(a.cycle_id)+' · '+fmt(a.volume_finance_kg)+' kg</option>';}).join('');STATE.calc=null;if($('sv2_submit'))$('sv2_submit').disabled=true;}
async function calculate(){var rt=$('sv2_rt').value,cycle=$('sv2_cycle').value,stock=num($('sv2_stock').value);if(!rt||!cycle||stock==null||stock<0)return toast('RT, cycle et stock RCN vérifié sont requis.','err');var r=await client().rpc('sacherie_calculer_plafond',{p_rt_id:rt,p_cycle_id:cycle,p_stock_rcn_kg:stock});if(r.error){STATE.calc=null;$('sv2_submit').disabled=true;return toast(friendly(r.error),'err');}STATE.calc=r.data;renderCalc();}
function renderCalc(){var x=STATE.calc,box=$('sv2_preview');if(!box||!x)return;var q=num($('sv2_qty').value)||0,after=Number(x.bags_already_held||0)+Number(x.reserved_approved_bags||0)+q,max=Number(x.max_new_available||0),ok=q>0&&q<=max;box.className='sv2-preview '+(ok?'':'bad');box.innerHTML='<b>Contrôle serveur</b><div class="sv2-kpi"><div><small>Volume financé</small><b>'+fmt(x.volume_finance_kg)+' kg</b></div><div><small>Acheté cycle</small><b>'+fmt(x.volume_achete_cycle_kg)+' kg</b></div><div><small>RCN vérifié</small><b>'+fmt(x.stock_rcn_kg_verified)+' kg</b></div><div><small>Plafond</small><b>'+fmt(x.system_max_bags)+' sacs</b></div></div>Déjà sous responsabilité RT : '+fmt(x.bags_already_held)+' · Réservé : '+fmt(x.reserved_approved_bags)+'<br>Encore disponible : <b>'+fmt(max)+' sacs</b> · Stock cluster : '+fmt(x.cluster_stock)+'<br>'+(q?('Après demande : <b>'+fmt(after)+' / '+fmt(x.system_max_bags)+'</b> '+(ok?'· CONFORME':'· DÉPASSEMENT SOP')):'Saisissez la quantité demandée.');$('sv2_submit').disabled=!ok;}
async function submitRequest(){var rt=$('sv2_rt').value,cycle=$('sv2_cycle').value,stock=num($('sv2_stock').value),qty=num($('sv2_qty').value);if(!rt||!cycle||stock==null||!(qty>0))return toast('Complétez RT, cycle, stock et quantité.','err');if(!STATE.calc||qty>Number(STATE.calc.max_new_available||0))return toast('Calculez d’abord un plafond conforme.','err');var clientId=uid('bagreq');if(!navigator.onLine){saveDraft(clientId,rt,cycle,stock,qty);return;}var r=await client().rpc('sacherie_creer_demande',{p_client_request_id:clientId,p_rt_id:rt,p_cycle_id:cycle,p_stock_rcn_kg:stock,p_requested_qty:Math.round(qty)});if(r.error){if(/fetch|network/i.test(String(r.error.message||''))){saveDraft(clientId,rt,cycle,stock,qty);return;}return toast(friendly(r.error),'err');}audit('bag_request_created',{request_id:r.data&&r.data.id,rt_id:rt,cycle_id:cycle,qty:qty});toast('Demande enregistrée. Elle attend maintenant la décision du Branch Manager.','ok');STATE.calc=null;await loadAll();MODE='requests';renderHost();}
function saveDraft(clientId,rt,cycle,stock,qty){var d=readDrafts();d.unshift({client_request_id:clientId,local_id:clientId,rt_id:rt,cycle_id:cycle,stock_rcn_kg_verified:stock,requested_qty:Math.round(qty),created_at:new Date().toISOString(),status:'DRAFT_LOCAL'});if(writeDrafts(d)){toast('Hors ligne : brouillon gardé localement. Il ne vaut pas approbation.','ok');}else toast('Impossible de sauvegarder le brouillon local.','err');}
async function syncDrafts(){if(!navigator.onLine)return toast('Réseau indisponible.','err');var d=readDrafts();if(!d.length)return;var left=[];for(var i=d.length-1;i>=0;i--){var x=d[i],clientId=x.client_request_id||x.local_id||uid('bagreq'),r=await client().rpc('sacherie_creer_demande',{p_client_request_id:clientId,p_rt_id:x.rt_id,p_cycle_id:x.cycle_id,p_stock_rcn_kg:x.stock_rcn_kg_verified,p_requested_qty:x.requested_qty});if(r.error){x.client_request_id=clientId;x.last_error=friendly(r.error);left.unshift(x);}else audit('bag_request_created',{request_id:r.data&&r.data.id,from_draft:true});}writeDrafts(left);toast(left.length?left.length+' brouillon(s) restent à corriger.':'Brouillons synchronisés et soumis au BM.',left.length?'err':'ok');await loadAll();}
function renderRequests(p){var drafts=readDrafts(),rows=STATE.requests,html='<div class="sv2-actions" style="margin-top:0"><button class="sv2-btn ghost" id="sv2_export_requests">Exporter CSV</button></div><div class="sv2-table"><table id="sv2_requests_table"><thead><tr><th>Demande</th><th>RT</th><th>Cycle</th><th>Plafond</th><th>Demandé</th><th>Ancienneté</th><th>Statut</th></tr></thead><tbody>';html+=drafts.map(function(d){return '<tr><td>'+esc(d.client_request_id||d.local_id)+'</td><td>'+esc(d.rt_id)+'</td><td>'+esc(d.cycle_id)+'</td><td>—</td><td>'+fmt(d.requested_qty)+'</td><td>'+age(d.created_at)+'</td><td>'+statusPill('DRAFT_LOCAL')+(d.last_error?'<br><small>'+esc(d.last_error)+'</small>':'')+'</td></tr>';}).join('');html+=rows.map(function(r){return '<tr><td><b>'+esc(r.request_code||String(r.id).slice(0,8))+'</b><br><small>'+esc(r.cluster||'')+'</small></td><td>'+esc(r.rt_nom||r.rt_id||'')+'</td><td>'+esc(r.cycle_id||'')+'</td><td>'+fmt(r.system_max_bags)+'</td><td>'+fmt(r.requested_qty)+'</td><td>'+age(r.requested_at||r.created_at)+'</td><td>'+statusPill(r.status)+'</td></tr>';}).join('');html+='</tbody></table></div>';if(!rows.length&&!drafts.length)html='<div class="sv2-empty">Aucune demande.</div>';p.innerHTML=html;if($('sv2_export_requests'))$('sv2_export_requests').onclick=function(){exportTable('sv2_requests_table','demandes');};}
function renderApproved(p){var rows=STATE.requests.filter(function(r){return r.status==='APPROVED';});if(!rows.length){p.innerHTML='<div class="sv2-empty">Aucune remise en attente.</div>';return;}p.innerHTML='<div class="sv2-table"><table><thead><tr><th>Demande</th><th>RT / cluster</th><th>Approuvé</th><th>Échéance</th><th>Action</th></tr></thead><tbody>'+rows.map(function(r){var soon=r.expires_at&&new Date(r.expires_at).getTime()-Date.now()<48*3600000;return '<tr><td><b>'+esc(r.request_code||r.id)+'</b></td><td>'+esc(r.rt_nom||r.rt_id)+'<br><small>'+esc(r.cluster||'')+'</small></td><td>'+fmt(r.approved_qty)+'</td><td>'+(soon?'<b style="color:#A83225">':'')+esc(remaining(r.expires_at))+(soon?'</b>':'')+'<br><small>'+esc((r.expires_at||'').replace('T',' ').slice(0,16))+'</small></td><td>'+(canExecuteRequest(r)?'<button class="sv2-btn" data-exec="'+esc(r.id)+'">Remettre</button>':'Lecture seule')+'</td></tr>';}).join('')+'</tbody></table></div>';p.querySelectorAll('[data-exec]').forEach(function(b){b.onclick=function(){executeRequest(b.dataset.exec);};});}
async function executeRequest(id){var r=STATE.requests.find(function(x){return String(x.id)===String(id);});if(!r||!canExecuteRequest(r))return toast('Votre fonction ne permet pas cette remise pour ce cluster.','err');var q=prompt('Quantité réellement remise (max '+r.approved_qty+') :',String(r.approved_qty));if(q===null)return;q=Math.round(num(q)||0);if(!(q>0)||q>Number(r.approved_qty||0))return toast('Quantité invalide ou supérieure à l’approbation.','err');if(!confirm('Confirmer la remise physique de '+q+' sacs à '+(r.rt_nom||r.rt_id)+' ? Le reliquat éventuel sera annulé.'))return;var res=await client().rpc('sacherie_executer_demande',{p_request_id:id,p_executed_qty:q,p_bag_state:'EMPTY',p_lot_id:null});if(res.error)return toast(friendly(res.error),'err');audit('bag_movement_executed',{request_id:id,executed_qty:q,bag_movement_code:res.data&&res.data.bag_movement_code});toast('Remise enregistrée : '+(res.data&&res.data.bag_movement_code||'mouvement créé')+'.','ok');await loadAll();}
function renderApprovals(p){if(!isBM()){p.innerHTML='<div class="sv2-empty">Réservé au Branch Manager.</div>';return;}var rows=STATE.requests.filter(function(r){return r.status==='PENDING_BM'||r.status==='HOLD';});if(!rows.length){p.innerHTML='<div class="sv2-empty">Aucune demande à décider.</div>';return;}p.innerHTML='<div class="sv2-table"><table><thead><tr><th>RT / cycle</th><th>Contrôle</th><th>Demande</th><th>Ancienneté</th><th>Actions</th></tr></thead><tbody>'+rows.map(function(r){return '<tr><td><b>'+esc(r.rt_nom||r.rt_id)+'</b><br>'+esc(r.cluster||'')+' · '+esc(r.cycle_id||'')+'</td><td>RCN vérifié '+fmt(r.stock_rcn_kg_verified)+' kg<br>Plafond '+fmt(r.system_max_bags)+' · Détenus '+fmt(r.bags_already_held)+' · Réservés '+fmt(r.reserved_approved_bags)+'<br><b>Disponible '+fmt(r.max_new_available)+'</b></td><td><b>'+fmt(r.requested_qty)+' sacs</b></td><td>'+age(r.requested_at||r.created_at)+'</td><td><button class="sv2-btn" data-ap="'+esc(r.id)+'">Approuver</button> <button class="sv2-btn warn" data-red="'+esc(r.id)+'">Réduire</button> <button class="sv2-btn ghost" data-hold="'+esc(r.id)+'">HOLD</button> <button class="sv2-btn danger" data-rej="'+esc(r.id)+'">Rejeter</button></td></tr>';}).join('')+'</tbody></table></div>';p.querySelectorAll('[data-ap]').forEach(function(b){b.onclick=function(){decision(b.dataset.ap,'APPROVE',null);};});p.querySelectorAll('[data-red]').forEach(function(b){b.onclick=function(){var r=STATE.requests.find(function(x){return String(x.id)===String(b.dataset.red);}),q=prompt('Quantité à approuver (max '+r.requested_qty+') :',String(r.requested_qty));if(q!==null)decision(b.dataset.red,'APPROVE',Math.round(num(q)||0));};});p.querySelectorAll('[data-hold]').forEach(function(b){b.onclick=function(){decision(b.dataset.hold,'HOLD',null);};});p.querySelectorAll('[data-rej]').forEach(function(b){b.onclick=function(){decision(b.dataset.rej,'REJECT',null);};});}
async function decision(id,action,qty){var comment='';if(action!=='APPROVE'){comment=prompt('Motif obligatoire :','')||'';if(!comment.trim())return toast('Un motif est obligatoire pour HOLD ou rejet.','err');}var res=await client().rpc('sacherie_decider_demande',{p_request_id:id,p_action:action,p_approved_qty:qty,p_comment:comment});if(res.error)return toast(friendly(res.error),'err');audit(action==='APPROVE'?'bag_request_approved':action==='HOLD'?'bag_request_hold':'bag_request_rejected',{request_id:id,approved_qty:qty});toast(action==='APPROVE'?'Demande approuvée.':'Décision enregistrée.','ok');await loadAll();}
function renderSettingsInto(host){if(!host)return;css();if(!isBM()){host.innerHTML='';return;}var configured=STATE.avances.filter(function(a){return a.cycle_id;});host.innerHTML='<section class="sv2-shell"><div class="sv2-headline"><div><h2>Paramètres de contrôle</h2><small>Cycles financés et règles opérationnelles</small></div></div><div class="sv2-grid"><div class="sv2-field"><label>RT</label><select id="sv2_c_rt">'+rtOptions()+'</select></div><div class="sv2-field"><label>Avance Cash existante</label><select id="sv2_av"><option value="">Choisir un RT</option></select></div><div class="sv2-field"><label>Cycle ID</label><input id="sv2_cycle_code" placeholder="Ex. WAVE-2027-BEO-001"></div><div class="sv2-field"><label>Volume financé autorisé (kg)</label><input id="sv2_cycle_vol" type="number" min="1" step="1"></div><div class="sv2-field"><label>Prix de référence (FCFA/kg)</label><input id="sv2_cycle_prix" type="number" min="1" step="1"></div></div><div class="sv2-actions"><button class="sv2-btn" id="sv2_cycle_save">Ouvrir le cycle</button></div><div class="sv2-note">Un seul cycle Sacherie peut être OPEN par RT. Les seuils d’inventaire restent gouvernés par les données serveur lorsqu’ils sont fournis ; le client n’invente pas de tolérance métier.</div>'+(configured.length?'<div class="sv2-table" style="margin-top:14px"><table><thead><tr><th>RT</th><th>Cycle</th><th>Volume</th><th>Statut</th><th>Action</th></tr></thead><tbody>'+configured.map(function(a){return '<tr><td>'+esc(a.rt_nom||a.rt_id||'')+'</td><td><b>'+esc(a.cycle_id)+'</b></td><td>'+fmt(a.volume_finance_kg)+' kg</td><td>'+statusPill(a.cycle_statut||'-')+'</td><td>'+(a.cycle_statut==='OPEN'?'<button class="sv2-btn ghost" data-close="'+esc(a.cycle_id)+'">Clôturer</button>':'—')+'</td></tr>';}).join('')+'</tbody></table></div>':'')+'</section>';$('sv2_c_rt').onchange=function(){var rt=this.value,avs=STATE.avances.filter(function(a){return String(a.rt_id)===String(rt)&&!a.cycle_id;});$('sv2_av').innerHTML='<option value="">Choisir une avance non affectée</option>'+avs.map(function(a){return '<option value="'+esc(a.id)+'">'+esc(a.date||'')+' · '+fmt(a.montant)+' FCFA</option>';}).join('');};$('sv2_cycle_save').onclick=saveCycle;host.querySelectorAll('[data-close]').forEach(function(b){b.onclick=function(){closeCycle(b.dataset.close);};});}
async function saveCycle(){var avance=$('sv2_av').value,code=$('sv2_cycle_code').value.trim().toUpperCase(),vol=num($('sv2_cycle_vol').value),prix=num($('sv2_cycle_prix').value);if(!avance||!code||!(vol>0))return;var r=await client().rpc('sacherie_configurer_cycle',{p_avance_id:avance,p_cycle_id:code,p_volume_finance_kg:vol,p_prix_reference_kg:prix});if(r.error)return alert(friendly(r.error));audit('bag_financing_cycle_configured',{avance_id:avance,cycle_id:code,volume_finance_kg:vol});await loadAll();if($('ctSettingsHost'))renderSettingsInto($('ctSettingsHost'));}
async function closeCycle(code){if(!confirm('Clôturer le cycle '+code+' ? Les nouveaux achats ne pourront plus y être rattachés automatiquement.'))return;var r=await client().rpc('sacherie_cloturer_cycle',{p_cycle_id:code});if(r.error)return alert(friendly(r.error));audit('bag_financing_cycle_closed',{cycle_id:code});await loadAll();if($('ctSettingsHost'))renderSettingsInto($('ctSettingsHost'));}
function showDecisions(kind){MODE=kind==='expiring'?'approved':'decisions';if(window.ANAGROCI_SACHERIE_CT)window.ANAGROCI_SACHERIE_CT.setTab('flux');if(HOST)renderHost();}
function openRequest(context){STATE.prefill=context||{};MODE='request';if(window.ANAGROCI_SACHERIE_CT)window.ANAGROCI_SACHERIE_CT.setTab('flux');setTimeout(function(){var h=$('ctFluxWorkflow');if(h)renderInto(h);},0);}
function exportTable(id,name){var table=$(id);if(!table)return;var rows=Array.from(table.querySelectorAll('tr')).map(function(tr){return Array.from(tr.children).map(function(c){return '"'+String(c.innerText||'').replace(/"/g,'""')+'"';}).join(';');}).join('\n');var blob=new Blob(['\ufeff'+rows],{type:'text/csv;charset=utf-8'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='sacherie-'+name+'-'+new Date().toISOString().slice(0,10)+'.csv';a.click();setTimeout(function(){URL.revokeObjectURL(a.href);},1000);audit('sacherie_export',{vue:name});}
function start(auth){AUTH=auth||window.ANAGROCI_AUTH;MODULE=(AUTH&&AUTH.module)||window.ANAGROCI_MODULE||MODULE;if(MODULE==='sacs'){css();loadAll();window.addEventListener('online',function(){syncDrafts();loadAll();});}}
window.ANAGROCI_SACHERIE_V2={renderInto:renderInto,renderSettingsInto:renderSettingsInto,showDecisions:showDecisions,openRequest:openRequest,reload:loadAll,isBM:isBM,esc:esc};
document.addEventListener('anagroci:authenticated',function(e){setTimeout(function(){start(e.detail);},100);});if(window.ANAGROCI_AUTH)setTimeout(function(){start(window.ANAGROCI_AUTH);},100);else if(MODULE==='sacs')setTimeout(function(){start(window.ANAGROCI_AUTH);},300);
})();
