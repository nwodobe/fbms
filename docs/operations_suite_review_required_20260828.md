# Human review required

Repository policy requires human review for this change because it modifies the root portal and adds business `.html` / JavaScript application surfaces. The pull request must remain open until CI and manual UAT are reviewed. Auto-merge is not appropriate for this branch.
